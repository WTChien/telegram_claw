version: "3.9"

services:
  nanoclaw:
    build: .
    container_name: nanoclaw
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
